# Contributing

1. Create a feature branch.
2. Keep gameplay logic outside composables where practical.
3. Add or update tests for game-engine, drawing-engine, recognition, or persistence changes.
4. Run `./gradlew testDebugUnitTest lintDebug assembleDebug` before opening a pull request.
5. Do not commit signing keys, passwords, local SDK paths, or generated build folders.
